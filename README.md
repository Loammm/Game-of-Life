# Conway's Game of Life

A C++ implementation of Conway's Game of Life, built from scratch as a learning project.

## Structure

- **`src/`** — The project source code (built step-by-step)
- **`vendored/SDL/`** — SDL3 source code (vendored for ease of use)
- **`CMakeLists.txt`** — CMake project file